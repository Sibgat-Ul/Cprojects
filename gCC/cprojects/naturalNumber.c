#include <stdio.h>

int main () {
    int n, i, odd = 1;

    for (i = 0; i < 10; i++) {

        printf("%d\n", odd);
        odd += 2;

    }

    return 0;
}


/*
int n, i;
    float sum = 0.0;
    float avg;
    i = 1;

    scanf("%d", &n);

    while (i <= n) {
        sum += i;
        i++;
    }

    avg = sum/n;

    printf("The sum is %.f avg is %.2f", sum, avg);


*/

/*

int n, i, m = 1;

    for (i = 1; i <= 5; i++) {
        m = (i*i*i);

        printf("%d\n", i*i*i);
    }

*/
